<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <mask id="h-cutout-sym">
      <!-- White canvas keeps the yellow shape visible -->
      <rect width="100" height="100" fill="white" />
      
      <!-- Black shapes cut holes in the yellow shape -->
      <g fill="black">
        <!-- Left vertical bar -->
        <rect x="28" y="0" width="13" height="100" />
        <!-- Right vertical bar -->
        <rect x="59" y="0" width="13" height="100" />
        <!-- Horizontal bar -->
        <rect x="28" y="42" width="42" height="13" />
      </g>
    </mask>
  </defs>

  <!-- Symmetrical Rhombus (Diamond) Shape -->
  <polygon points="50,5 95,50 50,95 5,50" 
           fill="#fcea1e" 
           mask="url(#h-cutout-sym)" />
</svg>